import { createStore } from "vuex";
import transport from "@/store/modules/transport";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    transport: transport,
  },
});
